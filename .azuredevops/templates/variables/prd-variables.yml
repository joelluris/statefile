# Production Environment Variables
# Used ONLY when deploying production resources (environment: prd)

variables:
  - name: SUBSCRIPTION_ID_prd
    value: 'TBD'
  
  - name: ENVIRONMENT_NAME
    value: 'prd'
  
  - name: LOCATION
    value: 'uaenorth'
